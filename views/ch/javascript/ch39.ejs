<% include head2.ejs %>

<h5><strong>&nbsp;Else if Statements</strong></h5>
<p>

    The else if statement specifies a block of code to be executed if the condition is true.
    <br><br>

    Example : <br>
    <code>if (condition-1){</code><br>
    <code>&nbsp;&nbsp;// code 1</code><br>
    <code>}</code>
    <br><br>
    <code>else if (condition-2){</code><br>
    <code>&nbsp;&nbsp;// code 2</code><br>
    <code>}</code>
    <br><br>
    <code>else{</code><br>
    <code>&nbsp;&nbsp;// code </code><br>
    <code>}</code>
    <br><br>

<h5><strong>&nbsp;Task</strong></h5>
<br>

<a id="t1"><i class="glyphicon glyphicon-remove"></i></a> Set value of x to <code>60</code> if y is equal to <code>20</code>.<br>

</p>

<% include base2.ejs %>
