<% include head4.ejs %>

<h5><strong>&nbsp;jQuery Event methods </strong></h5>
<p>

    The <code>mouseenter()</code> method attaches an event handler function to an element.
    <br><br>
    The function is executed when the mouse pointer enters a element.
    <br><br>
    Examples : <br><br>
    <code>$("p").mouseenter(function(){</code><br>
    <code>&nbsp;&nbsp;// handle the event</code><br>
    <code>});</code><br>
    When the mouse enters the paragraph area, this function will be executed.
    <br><br>

<h5><strong>&nbsp;Task</strong></h5>

<a id="t1"><i class="glyphicon glyphicon-remove"></i></a> Hide h3 on <code>mouse enter</code>.

</p>

<% include base4.ejs %>
